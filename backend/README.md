# CURAVERIS HEALTHCARE PRIVATE LIMITED Backend

PHP + MySQL API for the CURAVERIS HEALTHCARE PRIVATE LIMITED website.

## Setup

1. Create/import `database/schema.sql` in MySQL.
2. Copy `.env.example` to `.env` and update credentials.
3. Run: `php -S localhost:8000 -t backend`

